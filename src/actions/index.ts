/**
 * Server Actions Index
 *
 * Central export for all Server Actions
 */

// Auth actions
export { login, register, logout } from './auth'

// Contact actions
export { createContact, updateContact, deleteContact } from './contacts'
